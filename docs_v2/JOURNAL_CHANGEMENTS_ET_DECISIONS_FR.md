# Journal des Changements et Décisions (V2)

Ce document trace toutes les évolutions architecturales majeures et les décisions techniques prises pour la version 2 du projet (Conformité NF EN 9100).

**Légende des Composants :**
- ⚙️ **[OT - OPC UA]** : Serveur de données industrielles (Simulateur d'usine `opcua_server.py`)
- 🌐 **[IT - Web]** : Serveur de supervision et dashboard (FastAPI `web_server.py`)
- 🛡️ **[Global]** : Architecture, Sécurité transverse, Certificats, ou Documentation

| ID | Date | Composant | Description du Changement / Décision | Raison / Justification |
| :---: | :---: | :--- | :--- | :--- |
| **CHG-V2-001** | 2026-04-18 | 🛡️ [Global] | Migration d'un POC monolithique vers une architecture distribuée (Serveur OPC UA simulant l'usine + Serveur Web de Supervision FastAPI). | Respecter le Modèle de Purdue (séparation des réseaux OT et IT). |
| **CHG-V2-002** | 2026-04-18 | 🌐 [IT - Web] | Suppression totale des capacités de contrôle à distance (démarrer, armer, acquitter). L'interface Web est strictement en **Lecture Seule**. | Atténuation des risques d'accidents liés à un mouvement robotique non désiré (Exigence NF EN 9100 Chap. 6.1 et 8.1.1). |
| **CHG-V2-003** | 2026-04-18 | 🛡️ [Global] | Implémentation d'une PKI locale (Public Key Infrastructure) générant des certificats SSL/TLS auto-signés. | Chiffrer les flux de données OPC UA (Basic256Sha256) et les flux Web (HTTPS). |
| **CHG-V2-004** | 2026-04-18 | 🛡️ [Global] | Reprise de la tenue des journaux de traçabilité (Changements, Décisions, Erreurs) avec distinction OT/IT automatisée. | Assurer la traçabilité continue pour les audits de qualité. |
| **CHG-V2-005** | 2026-04-18 | 🛡️ [Global] | Modification du générateur de certificat (`generate_certs.py`) pour inclure l'IP `127.0.0.1` en plus de `localhost` dans les SANs (Subject Alternative Names). Ajout du certificat Web au magasin d'autorités de confiance (Root) de Windows via `certutil`. | Supprimer l'alerte bloquante "Votre connexion n'est pas privée" sur le navigateur local pour une meilleure expérience utilisateur. |
| **CHG-V2-006** | 2026-04-18 | ⚙️ [OT - OPC UA] | Ajout d'une nouvelle variable simulée (`VitesseBras` en %) dans le modèle de données du serveur OPC UA, conditionnée par l'état de la cellule (0% si en défaut). | Enrichir le jumeau numérique avec des métriques industrielles réalistes. |
| **CHG-V2-007** | 2026-04-18 | 🌐 [IT - Web] | Mise à jour du client OPC UA (FastAPI) et du Dashboard HTML pour requêter et afficher la nouvelle métrique de vitesse. | Maintenir la parité entre l'usine (OT) et la supervision (IT). |
| **CHG-V2-008** | 2026-04-18 | ⚙️ [OT - OPC UA] | Ajout d'une troisième cellule robotisée au simulateur. Enrichissement majeur du modèle de données : Type de robot, Progression du cycle, Alarmes actives, Temps avant maintenance. | Représenter un cas d'usage plus réaliste et complexe (3 types de robots différents). |
| **CHG-V2-009** | 2026-04-18 | 🌐 [IT - Web] | Implémentation d'un système d'authentification par Token JWT (Opérateur vs Maintenance). Création d'une page de login. | Contrôler l'accès aux données sensibles et personnaliser l'affichage. |
| **CHG-V2-010** | 2026-04-18 | 🌐 [IT - Web] | Création d'un workflow de maintenance conditionnel : l'Opérateur voit un bouton "Appeler la maintenance" quand une machine est usée, la Maintenance voit l'historique des appels. Refonte du dashboard avec barres de progression. | Séparer les responsabilités selon les rôles industriels standard. |
| **CHG-V2-011** | 2026-04-18 | 🌐 [IT - Web] | Amélioration visuelle (UI/UX) du Dashboard : Le type de robot est affiché dans un "badge" gris, la barre de progression passe au rouge lors d'une alarme, et les alarmes sont mises en évidence dans une boîte d'alerte dédiée. | Faciliter la lecture rapide par les opérateurs dans un environnement industriel (Management Visuel). |
| **CHG-V2-012** | 2026-04-18 | ⚙️ [OT - OPC UA] | Les variables OPC UA sont passées en `writable` et la génération d'alarmes aléatoires est désactivée. | Permettre au serveur Web d'injecter des données pour simuler des scénarios (POC) de manière contrôlée. |
| **CHG-V2-013** | 2026-04-18 | 🌐 [IT - Web] | Ajout d'un "Panneau de Simulation" avec boutons interactifs pour forcer des défauts, avancer le compteur de maintenance, acquitter les erreurs et réaliser les maintenances. | Fournir un outil de test pour la soutenance du projet sans avoir à modifier le code Python. |
| **CHG-V2-014** | 2026-04-18 | 🌐 [IT - Web] | Création d'une "Bannière d'Alerte Maintenance" (rouge) visible uniquement par le rôle MAINTENANCE. Elle s'affiche automatiquement si le compteur d'une machine tombe sous les 10 Heures ou si l'Opérateur clique sur son bouton d'appel. | Répondre au besoin de proactivité (Alerte préventive < 10H) sans forcer l'action humaine. |
| **CHG-V2-015** | 2026-04-18 | ⚙️ [OT - OPC UA] | Ajout de nouvelles variables OPC UA pour simuler des sous-systèmes industriels : Pression Pneumatique, Etat du Bridage, Niveau de Lubrifiant (Lubrix), IP de la cellule et Nom de la cellule. | Fournir les données nécessaires à un affichage de supervision de type "Terminal / Dashboard Industriel". |
| **CHG-V2-016** | 2026-04-18 | 🌐 [IT - Web] | Refonte de l'interface des cellules dans le Dashboard Web pour adopter un style "Terminal" en police à chasse fixe (monospace), avec intégration de pastilles de statut (🟢/🔴) pour le robot, la pneumatique et le lubrifiant, ainsi que l'état du certificat SSL. | Améliorer le réalisme de l'interface de supervision en s'inspirant des IHM industrielles classiques, à la demande de l'utilisateur. |
| **CHG-V2-017** | 2026-04-18 | ⚙️ [OT - OPC UA] | Ajout de variables de diagnostic approfondi (`TemperatureAxes`, `NiveauVibration`) qui évoluent dynamiquement lorsque la machine est en production. | Fournir des données prédictives d'usure réservées aux équipes techniques. |
| **CHG-V2-018** | 2026-04-18 | 🌐 [IT - Web] | Création d'une vue étendue pour le rôle MAINTENANCE : affichage des diagnostics avancés (Température, Vibrations) et intégration des boutons d'action ("Acquitter Défaut", "Effectuer Maintenance") directement sur la carte de chaque cellule. | Donner aux techniciens de maintenance des outils d'intervention rapide directement sur la supervision, sans passer par le panneau de test général. |
| **CHG-V2-019** | 2026-04-18 | 🌐 [IT - Web] | Amélioration du workflow de demande d'intervention : L'opérateur peut désormais cliquer sur un bouton pour alerter la maintenance immédiatement lorsqu'un défaut survient sur une machine (en plus de l'alerte de temps < 10H). | Permettre une réactivité immédiate de l'opérateur en cas de panne (management des incidents). |
| **CHG-V2-020** | 2026-04-18 | 🌐 [IT - Web] | La bannière d'alerte rouge de la maintenance précise désormais la raison de l'appel ("Défaut critique" ou "Maintenance préventive"). Elle ne s'affiche plus de manière automatique : l'opérateur **doit** cliquer sur le bouton de demande pour alerter la maintenance. | Aider le technicien à prioriser ses interventions et redonner la responsabilité d'appel à l'opérateur. |
| **CHG-V2-021** | 2026-04-19 | 🌐 [IT - Web] | Restauration de l'alerte automatique MAINTENANCE sur seuil de maintenance préventive : la bannière `#maint-alerts-banner` s'affiche quand `HeuresAvantMaintenance <= 10` (ou `MaintenanceRequise`), en plus des alertes d'appel opérateur. | Assurer la proactivité de la maintenance (préventif < 10H) après suppression accidentelle de la logique lors de modifications successives du dashboard. |
| **CHG-V2-022** | 2026-04-19 | 🛡️ [Global] | Introduction d'un socle de qualité de code : ajout de `pyproject.toml` (config Black/Ruff/Pytest) et ajout des dépendances d'outillage (`ruff`, `black`, `pytest-cov`). | Standardiser le style (PEP8), réduire les régressions et fournir des preuves qualité (RNCP / audits). |
| **CHG-V2-023** | 2026-04-19 | 🛡️ [Global] | Durcissement de la gestion des secrets et de l'authentification : la clé JWT est désormais configurable via variable d'environnement `SRC_JWT_SECRET_KEY` et la vérification de mot de passe s'appuie sur `passlib[bcrypt]` quand disponible (fallback POC contrôlé). | Alignement OWASP : éviter les secrets en dur et renforcer l'authentification, tout en conservant un mode démo utilisable. |
| **CHG-V2-024** | 2026-04-19 | 🌐 [IT - Web] | Restriction de l'API de simulation `/api/simu/action` au rôle `MAINTENANCE` (RBAC). Refactor PEP8 + robustesse (logging, exceptions). | Réduire la surface d'attaque (OWASP) et renforcer la séparation des responsabilités (principe du moindre privilège). |
| **CHG-V2-025** | 2026-04-19 | 🛡️ [Global] | Ajout de tests unitaires (pytest) pour les composants critiques (JWT, rôles, endpoints FastAPI) avec mocking des lectures OPC UA. | Qualité/ISO : preuve de non-régression et validation systématique. |
| **CHG-V2-026** | 2026-04-19 | 🛡️ [Global] | Normalisation des imports Python : suppression du `sys.path.append(...)` et utilisation d'un import package explicite `src_v2.security` dans `web_server.py`. | Améliorer la conformité PEP8, la maintenabilité et la compatibilité outillage (tests/lint) en évitant les hacks de chemin. |
| **CHG-V2-027** | 2026-04-19 | 🌐 [IT - Web] | Standardisation du lancement Uvicorn : passage à la cible `src_v2.web_server:app` pour un démarrage compatible avec les imports package. | Rendre le démarrage plus robuste et conforme aux conventions Python (exécution en module/package). |
| **CHG-V2-028** | 2026-04-19 | 🛡️ [Global] | Extension des tests unitaires RBAC : ajout de scénarios sur la demande de maintenance (opérateur) et la consultation d'historique (maintenance), avec vérification des interdictions (opérateur). | Augmenter la couverture fonctionnelle et sécuritaire (preuves de contrôle d'accès et de non-régression). |
| **CHG-V2-029** | 2026-04-19 | 🛡️ [Global] | Objectif couverture atteint sur les modules IT testables : configuration `coverage` (omit du serveur OT `opcua_server.py`) et ajout de tests couvrant les branches critiques (login erreurs, fetch OPC UA via fakes/mocks, branches `/api/simu/action`, root HTML). | Viser un seuil de qualité mesurable (>80%) sur la supervision Web/sécurité, tout en excluant les boucles serveur OT non pertinentes pour les tests unitaires. |
| **CHG-V2-030** | 2026-04-19 | 🌐 [IT - Web] | Correction de compatibilité Starlette/Jinja2 : mise à jour de l'appel `TemplateResponse` pour utiliser la signature `TemplateResponse(request, name)` et supprimer le warning de dépréciation. | Préparer la montée de versions des dépendances et maintenir une exécution propre (sans warnings). |
| **CHG-V2-031** | 2026-04-19 | 🛡️ [Global] | Ajout de scripts PowerShell de démonstration et de qualité : `run_servers.ps1` (certs + serveurs OPC UA/Web) et `run_quality.ps1` (tests, coverage, lint, format). | Simplifier l'exécution "one-click" pour la soutenance et fournir des preuves qualité reproductibles. |
| **CHG-V2-032** | 2026-04-22 | 🛡️ [Global] | Mise à jour du `README.md` : ajout de sections "Prérequis & installation", "Sécurité & certificats (SSL/TLS)", "Architecture du projet", "Lancement" et "Qualité" avec commandes reproductibles. | Renforcer les preuves de Compétence 2 (code normé + projet portable) et Compétence 8 (sécurisation des échanges) via une documentation exécutable. |
| **CHG-V2-033** | 2026-04-22 | 🛡️ [Global] | Introduction de PyLint et ajout d'une configuration `.pylintrc` pour un reporting qualité adapté au projet (FastAPI/OT simulation) et reproductible. | Fournir une preuve supplémentaire de qualité logicielle (analyse statique) et un score PyLint comparable en soutenance. |
| **CHG-V2-034** | 2026-07-31 | 🛡️ [Global] | Ajout de locustfile.py pour les tests de charge (50 users concurrents). | Valider les performances du serveur web sous charge (C1.6 CDAN). |
| **CHG-V2-035** | 2026-07-31 | 🌐 [IT - Web] | Améliorations accessibilité dashboard (audit Lighthouse 82/100) : ajout du landmark <main>, correction hiérarchie des titres (h2→h1 sur l'écran de login), amélioration du ratio de contraste des textes secondaires (#7f8c8d → #5a6a6b). | Conformité RGAA / WCAG AA — réponse audit Lighthouse (C1.7 CDAN). |
| **CHG-V2-036** | 2026-08-08 | 🛡️ [Global] | Remplacement du stockage en mémoire par une base SQLite réelle (`src_v2/db.py`, SQLModel) : tables `Utilisateur` (comptes, hash bcrypt) et `HistoriqueMaintenance` (traçabilité des interventions), avec amorçage idempotent (`init_db()`) et injection par dépendance FastAPI (`get_session`). `active_maintenance_requests` reste en mémoire (état transitoire), seul l'historique est persisté. `security.py` et `web_server.py` adaptés en conséquence. | Combler l'écart identifié lors de l'audit blanc RNCP entre la dépendance `sqlmodel` déclarée (`requirements.txt`) et l'absence de toute base de données fonctionnelle dans le code — preuve concrète pour C2.4 (Modélisation de BDD). |
| **CHG-V2-037** | 2026-08-08 | 🛡️ [Global] | Ajout d'une contrainte d'intégrité référentielle réelle : `HistoriqueMaintenance.username_auteur` porte une clé étrangère SQL vers `Utilisateur.username`, avec activation explicite de `PRAGMA foreign_keys=ON` sur chaque connexion SQLite (sans quoi la contrainte serait purement déclarative et non appliquée). 4 tests de persistance ajoutés (`tests/test_db.py`), dont un qui prouve le rejet effectif d'un `username_auteur` inconnu (`IntegrityError`) ; `tests/conftest.py` créé pour isoler la base de test (`SRC_DB_PATH`). Suite complète : 21/21 tests passés (0 régression sur les 17 existants). | Preuve concrète et vérifiée pour C1.10 (Sécurité BDD — contraintes, triggers) : la contrainte n'est pas seulement déclarée dans le schéma, son application est testée. |
| **CHG-V2-038** | 2026-08-08 | 🛡️ [Global] | Correctif définitif de INC-V2-012 (voir JOURNAL_ERREURS_ET_FIXES_FR.md → INC-V2-014) : `verify_password()` n'utilise plus passlib mais `bcrypt.checkpw()` directement, ce qui élimine le bug de version passlib/bcrypt et le fallback en clair qu'il avait rendu nécessaire. `requirements.txt` mis à jour (`passlib[bcrypt]` retiré, `bcrypt` ajouté explicitement). 2 tests remplacés (`test_verify_password_real_bcrypt`, `test_verify_password_rejects_malformed_hash`) pour vérifier une comparaison bcrypt réelle plutôt que l'ancien contournement. Suite complète : 21/21 tests passés. | Fermer un risque de sécurité résiduel identifié lors de la revue de la base de données (CHG-V2-036/037) : le fallback en clair était actif par défaut et n'était jamais mentionné dans le dossier — preuve renforcée et honnête pour C1.8 (risques de sécurité) et C1.10 (sécurité des accès aux données). |
| **CHG-V2-039** | 2026-08-19 | 🛡️ [Global] | Extraction de la couche d'accès OPC UA (`src_v2/opcua_client.py`) hors du contrôleur web : les fonctions `fetch_opcua_data`, `fetch_axis_data` et `apply_simulated_action` (autrefois définies directement dans `web_server.py`) forment désormais un module "couche Service" dédié, que le contrôleur importe et appelle sans jamais manipuler un client OPC UA directement. | Réduire le couplage OT/IT et améliorer la testabilité et la lisibilité du contrôleur HTTP (Compétence 9 — Code propre et maintenable). |
| **CHG-V2-040** | 2026-08-19 | 🛡️ [Global] | Extension du schéma SQLite avec 3 nouvelles tables persistées : `MesureAxe` (relevés horodatés par axe moteur — température/courant/couple), `MesureCellule` (pression pneumatique, niveau lubrifiant) et `DefautHistorique` (historique des défauts, sévérité, statut résolu). Ajout d'une tâche de fond (`_sampling_loop`, intervalle 60s, démarrée/arrêtée via le `lifespan` FastAPI) qui échantillonne et persiste en continu les mesures OPC UA. | Constituer un historique exploitable (courbes de tendance, page de détail par cellule) plutôt qu'un instantané perdu à chaque rafraîchissement du dashboard — preuve concrète pour C2.4 (Modélisation de BDD) sur un cas d'usage de maintenance prédictive. |
| **CHG-V2-041** | 2026-08-19 | 🌐 [IT - Web] | Ajout de 3 nouveaux endpoints, tous réservés au rôle MAINTENANCE : `GET /api/cell/{id}/axes` (état instantané des 6 axes), `GET /api/cell/{id}/measures` (historique par plage horaire, 24h/7j/30j) et `GET /api/faults/history` (historique des défauts, filtrable par cellule via `?cell_id=`). | Exposer l'historique persisté (CHG-V2-040) à l'interface web pour les courbes de tendance et le suivi des défauts par cellule. |
| **CHG-V2-042** | 2026-08-19 | 🌐 [IT - Web] | Lecture réelle de la date d'expiration du certificat SSL (`_load_ssl_cert_expiry`, bibliothèque `cryptography.x509`) depuis `certs/web_cert.pem`, affichée sur le dashboard à la place d'une date fictive calculée côté client. | Fournir une preuve honnête et vérifiable de l'état de sécurité (Compétence 8 — sécurisation des échanges) plutôt qu'une donnée simulée. |
| **CHG-V2-043** | 2026-08-19 | 🛡️ [Global] | Refonte visuelle de l'interface à la demande de l'utilisateur : extraction d'un design system partagé (`static/theme.css`, `static/common.js`) chargé par toutes les pages, remplacement de la navigation par un menu déroulant (barre de navigation sombre inspirée du style envoyé par l'utilisateur, valcogroup-valves.com), et remplacement des popups `alert()` natifs par un composant modal personnalisé (`showModal`). | Interface plus professionnelle et visuellement cohérente sur l'ensemble du site, plutôt que du CSS dupliqué par page et des alertes navigateur génériques. |
| **CHG-V2-044** | 2026-08-19 | 🌐 [IT - Web] | Passage d'une page unique (défilement long) à une architecture de vraies pages séparées, à la demande de l'utilisateur : `/` (vue principale), `/cell/{id}` (détail cellule), `/historique-maintenance`, `/historique-pannes`, `/donnees` (vue comparative des 3 cellules). Le bloc "Diagnostics avancés" (température/vibration par axe), auparavant sur chaque carte de la vue principale, est retiré de cette vue et réservé à la page de détail. | Lisibilité (une carte de la vue principale ne doit garder que l'essentiel) et navigation plus claire pour un site à vocation multi-fonction (supervision, historiques, comparaison). |
| **CHG-V2-045** | 2026-08-19 | 🌐 [IT - Web] | Chart.js vendorisé localement (`static/chart.umd.js`, servi via `StaticFiles`) au lieu d'un chargement depuis un CDN externe (`cdnjs.cloudflare.com`). | Un poste de supervision industrielle peut fonctionner sur un réseau sans accès Internet sortant, volontairement cloisonné pour des raisons de sûreté OT ; une dépendance CDN externe est un point de défaillance inacceptable pour un outil de supervision (voir JOURNAL_ERREURS_ET_FIXES_FR.md → INC-V2-016). |
| **CHG-V2-046** | 2026-08-19 | 🛡️ [Global] | Ajout de `seed_historical_data.py`, script idempotent générant 3 semaines d'historique réaliste (mesures par axe, mesures cellule, défauts) pour la démonstration/soutenance. Un marqueur dédié (`SEED_MARKER`) inséré dans la description de chaque défaut de démonstration permet de le distinguer sans ambiguïté d'un défaut réellement déclenché par un utilisateur ; le script vérifie systématiquement l'état existant avant d'écrire et ne supprime ni ne duplique jamais de données déjà présentes, même relancé après un déploiement en production. | Fournir un jeu de données de démonstration réaliste pour la soutenance sans jamais risquer d'altérer ou dupliquer des données réelles déjà accumulées par le serveur déployé (voir aussi JOURNAL_ERREURS_ET_FIXES_FR.md → INC-V2-018 et INC-V2-019). |
| **CHG-V2-047** | 2026-08-20 | 🌐 [IT - Web] | Retrait, à la demande de l'utilisateur, de l'affichage du "Bridage" (état pneumatique `BridageActif`) sur les cartes de la vue principale, et du "Niveau de vibrations" sur le bloc "Diagnostics avancés" de la page de détail. Le point de couleur pneumatique reflète désormais si la pression est dans sa plage nominale (seuil 5.5 bar) plutôt que l'état de bridage. | Données jugées peu lisibles et peu réalistes pour un affichage de supervision destiné à un opérateur/technicien, sans plus-value métier claire dans ce contexte. |
| **CHG-V2-048** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un filtre par cellule sur l'historique de maintenance : `db.list_history(session, cellule_id=...)` et `GET /api/maintenance/history?cell_id=...` (rétrocompatible, le paramètre est optionnel). Affichage d'une carte "Historique Maintenance — cette cellule" sur la page de détail, en complément de l'historique des défauts déjà présent. Tests ajoutés côté DB (`test_list_history_filters_by_cellule_id`) et API (`test_maintenance_history_filters_by_cell_id`). | Réponse directe à la demande utilisateur : centraliser toute l'information d'une cellule (défauts, maintenance, mesures par axe) sur sa page de détail plutôt que de l'éparpiller entre plusieurs pages. |
| **CHG-V2-049** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un sélecteur d'axes (cases à cocher Axe 1 à 6, boutons "Tout"/"Aucun") sur les courbes température/courant/couple de la page de détail d'une cellule, permettant d'isoler un ou plusieurs axes sur les graphiques sans refaire d'appel réseau (filtrage côté client sur les données déjà chargées). | Réponse directe à la demande utilisateur : pouvoir comparer un axe précis sans être gêné visuellement par les 5 autres courbes superposées. |
| **CHG-V2-050** | 2026-08-20 | 🛡️ [Global] | Réorganisation de la racine du dépôt : les scripts ponctuels (`generate_certs.py`, `seed_historical_data.py`, `run_quality.ps1`, `run_servers.ps1`, `locustfile.py`) sont déplacés dans un nouveau dossier `scripts/`. `seed_historical_data.py` est adapté pour retrouver le paquet `src_v2` depuis son nouvel emplacement (résolution basée sur `__file__`, indépendante du répertoire courant) ; `run_quality.ps1` et `run_servers.ps1` se replacent eux-mêmes à la racine du dépôt via `$PSScriptRoot` avant de s'exécuter, pour rester utilisables depuis n'importe quel répertoire d'appel. README FR/EN mis à jour en conséquence (chemins, et nouvelle section "Tests de charge (Locust)" documentant `locustfile.py`, jusqu'ici non documenté). `locust` ajouté à `requirements.txt`. | Racine de dépôt plus lisible et plus professionnelle pour la présentation au jury (demande explicite de l'utilisateur), sans changer le comportement des scripts pour l'utilisateur final. |
| **CHG-V2-051** | 2026-08-20 | 🛡️ [Global] | Suppression de `_sharedStrings.xml` et `_sheet1.xml` (fragments XML orphelins d'un fichier Excel de suivi des compétences RNCP, dézippé par erreur au tout premier commit du dépôt, illisibles tels quels) et de `smart_robotic_cell_project.md` (fichier vidé de son contenu via l'interface web de GitHub avant le rebase du 2026-08-20, ne conservait plus qu'un caractère). | Nettoyage demandé par l'utilisateur : ces fichiers n'étaient pas exploitables et n'apportaient aucune valeur à la racine du dépôt consultée par le jury. |

| **CHG-V2-052** | 2026-08-20 | 🌐 [IT - Web] | Bandeau de navigation partagé (`renderTopNav`, `common.js`) : ajout d'un bouton FR/EN commun à toutes les pages (dashboard, détail cellule, historiques maintenance/défauts, comparaison de données), qui recharge la page après avoir basculé la langue stockée. Retrait du bouton FR/EN ad hoc du dashboard (devenu redondant). | Le bouton FR/EN n'existait auparavant que sur le dashboard principal : il disparaissait dès qu'on naviguait vers une autre page (défaut remonté par l'utilisateur). Le rechargement complet évite aussi un bug latent où le panneau de simulation (dessiné une seule fois pour ne pas clignoter) restait dans l'ancienne langue après bascule. |
| **CHG-V2-053** | 2026-08-20 | 🌐 [IT - Web] | `theme.css` : ajout de `text-decoration: none` sur `.btn`/`a.btn` (les liens stylés en bouton — "Retour au dashboard", "View detail →" — étaient soulignés par défaut du navigateur) et d'une règle `.range-select` autonome (le sélecteur de période de la page de détail cellule n'était pas dans un `.filter-bar` et n'héritait donc d'aucun style, contrairement au reste du site). | Incohérence visuelle remontée par l'utilisateur. |
| **CHG-V2-054** | 2026-08-20 | 🌐 [IT - Web] | Page de détail cellule (`cell_detail.html`) entièrement passée en i18n (dictionnaire FR/EN complet, comme les autres pages), à la demande de l'utilisateur. Titre remplacé par le vrai nom de la cellule (ex. "Cellule 1 — Perçage Aéro") au lieu de "Détail Cellule #1". Suppression de "— valeurs instantanées" (titre "Axes moteurs") et de "— cette cellule" (titres "Historique des défauts"/"Historique Maintenance"). | Lisibilité et cohérence linguistique demandées par l'utilisateur. |
| **CHG-V2-055** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un agrandissement plein écran par graphique (bouton "⤢" sur chaque carte de courbe, page de détail cellule) : nouvelle fenêtre modale générique (`openChartZoom`/`closeChartZoom` dans `common.js`) reconstruisant le graphique Chart.js avec les mêmes données sur un plus grand canvas. | Demande utilisateur : pouvoir isoler un type de courbe en grand format pour mieux le lire. |
| **CHG-V2-056** | 2026-08-20 | 🛡️ [Global] | Densité des courbes historiques augmentée à la demande de l'utilisateur : intervalle d'échantillonnage de la tâche de fond (`web_server.py`) réduit de 60 à 15 secondes ; intervalle du script `seed_historical_data.py` réduit de 15 à 5 minutes. | Courbes jugées trop clairsemées par l'utilisateur. Voir aussi INC-V2-020 pour la correction du modèle de production/arrêt du jeu de données historique, qui contribuait aussi à l'aspect erratique des courbes. |
| **CHG-V2-057** | 2026-08-20 | 🛡️ [Global] | Remplacement du booléen `DefautHistorique.resolu` par un statut à 3 états (`db.DEFAUT_STATUT_ACTIF` / `_EN_COURS` / `_RESOLU`). Ajout de `HistoriqueMaintenance.defaut_id` (clé étrangère vers le défaut concerné) et `HistoriqueMaintenance.probleme_resolu`, ainsi que des fonctions `db.get_defaut`, `db.set_defaut_statut` et `db.add_maintenance_intervention` (crée la ligne d'historique ET met à jour le statut du défaut en une seule opération transactionnelle). Aucune migration de schéma nécessaire : `smart_robotic_cell.db` est généré localement et exclu du dépôt (`.gitignore`), il suffit de le régénérer (`init_db()` + `seed_historical_data.py` si besoin). | Décision utilisateur explicite (item "Autre" de la demande d'évolutions) : plutôt que de créer une table `DemandeIntervention` séparée pour représenter une intervention en attente, un défaut au statut "actif" (jamais encore pris en charge) *est* la demande d'intervention implicite — plus simple, sans duplication d'état, et cohérent avec le fait qu'un défaut "en_cours" (pris en charge mais non résolu) doit rester visible comme tel plutôt que de redevenir une simple ligne binaire résolu/non résolu. |
| **CHG-V2-058** | 2026-08-20 | 🌐 [IT - Web] | Nouvel endpoint `POST /api/maintenance/intervention` (rôle MAINTENANCE) : la Maintenance choisit un défaut précis (identifiant retourné par `/api/faults/history`), déclare si le problème est résolu ou toujours actif, avec des notes libres optionnelles ; le défaut passe automatiquement à `resolu`/`en_cours` en conséquence. `GET /api/faults/history` étendu avec les filtres `fault_status`, `since`, `until` (en plus de `cell_id` déjà existant) ; `GET /api/maintenance/history` étendu avec `since`/`until` et renvoie désormais `defaut_id`/`probleme_resolu` par entrée. | Réponse directe aux items 5 et 6 ("filtrer par date, par nom de cellule, par problème actif ou non") et à l'item "Autre" ("la maintenance doit pouvoir préciser pour quel défaut elle est intervenue et si le problème reste actif, avec résolution automatique") de la demande d'évolutions utilisateur. |
| **CHG-V2-059** | 2026-08-20 | 🌐 [IT - Web] | Refonte de `fault_history.html` : filtres cellule (menu déroulant affichant le nom de la cellule, pas seulement son numéro — `cellDisplayName()` dans `common.js`), statut (actif/en cours/résolu/tous) et plage de dates, plus un bouton "Intervenir" par défaut non résolu ouvrant un formulaire modal (notes + choix résolu/reste actif) qui appelle `/api/maintenance/intervention`. Refonte de `maintenance_history.html` : mêmes filtres cellule/date, ajout d'un panneau "Demandes en attente" (défauts au statut "actif", masquable via une case à cocher) affiché au-dessus de l'historique des interventions déjà réalisées, et affichage du défaut lié + résultat sur chaque ligne d'intervention qui en référence un. `cell_detail.html` adapté au nouveau statut à 3 états (le mini-historique des défauts de la page de détail affichait auparavant seulement résolu/actif). | Réponse directe aux items 5 et 6 de la demande d'évolutions utilisateur ("filtrer par nom de cellule/date/problème actif", "voir aussi les demandes d'intervention", "menu de filtre joli avec le nom des cellules"). |
| **CHG-V2-060** | 2026-08-20 | 🌐 [IT - Web] | `common.js` enrichi de `CELL_NAMES`/`cellDisplayName()` (noms des 3 cellules, dupliqués depuis `opcua_server.py` pour un affichage de menu sans appel API supplémentaire) et `faultStatusTagClass()` (classe CSS selon le statut d'un défaut, réutilisée par `fault_history.html`, `maintenance_history.html` et `cell_detail.html`). `theme.css` : nouvelle couleur de statut "en cours" (`--warn`, orange, cohérente avec le reste du site), style du formulaire d'intervention (champ notes, choix radio), et bouton "Intervenir" (`.btn-intervene`). | Éviter la duplication de logique d'affichage de statut/nom de cellule entre les 3 pages qui en ont désormais besoin — cohérent avec le design system partagé introduit en CHG-V2-043. |
| **CHG-V2-061** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : le type et la description des défauts (`fault_history.html`, `maintenance_history.html`, `cell_detail.html`) restaient affichés en français même en mode EN. Ajout de `FAULT_TEXT_FR_TO_EN` (table de correspondance FR→EN) et `translateFaultText()` dans `common.js`, appliqués aux champs `type`/`description` des défauts sur les 3 pages concernées. | `type_defaut`/`description` proviennent d'un vocabulaire fermé et énumérable côté backend (8 `FAULT_TYPES` + le type de défaut manuel + le libellé du défaut forcé depuis le panneau de simulation) : une traduction par substitution de chaînes est fiable et sans risque de faux positif. Choix explicite de **ne pas** traduire `HistoriqueMaintenance.action` : ce champ contient des notes libres saisies par les utilisateurs (opérateur/maintenance), conservées telles qu'écrites dans la langue d'origine — cohérent avec le traitement de `action` comme trace d'audit immuable déjà acté en CHG-V2-057. |
| **CHG-V2-062** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : un Opérateur cliquant sur "Demander une intervention Maintenance" (`dashboard.html`) n'avait aucun moyen de préciser la nature du problème. Ajout d'un composant générique de saisie de texte dans `common.js` (`openTextPromptModal`/`closeTextPromptModal`/`submitTextPromptModal`, sur le même modèle que la popup d'information `showModal`/`closeModal` déjà existante) et d'un paramètre `message: str = ""` sur `POST /api/maintenance/request`, ajouté au texte de l'action journalisée s'il est renseigné. `callMaintenance(cellId)` ouvre désormais cette popup avant d'envoyer la requête ; le champ reste optionnel (une demande sans message reste possible, comme avant). | Réponse directe à la demande utilisateur ("en tant qu'opérateur, je ne peux pas mettre de message"). Composant conçu comme générique (titre/placeholder/callback paramétrables) plutôt que spécifique à ce seul usage, pour pouvoir être réutilisé par d'autres popups de saisie libre à l'avenir sans dupliquer le HTML/CSS d'une fenêtre modale. |
| **CHG-V2-063** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : une demande d'intervention générique (bouton "Demander une intervention", non liée à un défaut précis) n'apparaissait pas dans le panneau "Demandes en attente" de `maintenance_history.html`, qui n'interrogeait que les défauts actifs (`/api/faults/history?fault_status=actif`). Nouvel endpoint `POST /api/maintenance/acknowledge-request` (rôle MAINTENANCE, paramètre `cell_id`) qui retire la cellule de `active_maintenance_requests` et journalise la prise en charge. `maintenance_history.html` interroge désormais aussi `GET /api/status` (déjà existant, expose `maint_requests`) et affiche ces demandes génériques dans un second panneau distinct "Demandes génériques (sans défaut associé)", chacune avec un bouton "Prise en charge". | Les deux signaux de demande en attente (défaut actif vs. appel générique opérateur) reposent sur des structures de données indépendantes et n'ont pas de correspondance 1:1 (`active_maintenance_requests` n'a pas d'horodatage ni de lien vers un défaut) — les fusionner dans une seule liste aurait nécessité une reconciliation artificielle. Un second panneau clairement étiqueté évite toute ambiguïté sur l'origine de chaque demande, tout en la rendant enfin visible comme le demandait l'utilisateur. |
| **CHG-V2-064** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : le message qu'un Opérateur saisit en demandant une intervention (CHG-V2-062) restait invisible pour la Maintenance tant que la demande n'était pas traitée — seul l'historique final le montrait, pas le panneau "Demandes génériques" lui-même. `active_maintenance_requests` (`web_server.py`) passe de `dict[int, str]` (nom d'utilisateur seul) à `dict[int, dict]` avec `{"username", "message"}`, affiché directement sur chaque ligne du panneau. `POST /api/maintenance/acknowledge-request` gagne aussi un paramètre `message` optionnel : la Maintenance peut désormais préciser ses propres notes en prenant en charge une demande, reprises avec le message d'origine de l'opérateur dans la même ligne d'historique ("Prise en charge ... (message opérateur : ...). Notes maintenance : ..."). | Réponse directe à l'utilisateur ("la maintenance ne voit pas le message" / "elle ne peut pas préciser de message si elle effectue la maintenance"). Garder le message sur la structure en mémoire (plutôt que de le reconstruire depuis l'historique) évite une dépendance croisée entre `active_maintenance_requests` et `HistoriqueMaintenance` pour un simple affichage. |
| **CHG-V2-065** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : la Maintenance ne pouvait qu'acquitter/traiter des défauts ou demandes déjà existants, jamais signaler elle-même un problème qu'elle constate sur une cellule. Nouvel endpoint `POST /api/maintenance/report-issue` (rôle MAINTENANCE ; `cell_id`, `description`, `severity` parmi `db.DEFAUT_SEVERITES`) créant un défaut réel via `db.add_defaut` avec un type fixe `"Anomalie signalée par la Maintenance"` (traduit en EN, cf. CHG-V2-061). Bouton "Signaler un problème" ajouté au 1er encadré ("Diagnostics avancés") de `cell_detail.html`, avec un petit formulaire modal (sévérité + description libre) ; ce même encadré affiche désormais aussi les problèmes actifs de la cellule (défauts non résolus), pour que l'information soit visible au même endroit qu'elle est signalée. | Réponse directe à l'utilisateur ("elle ne peut pas afficher qu'il y a un pb ou défaut sur une cellule" + "j'aimerais que cela soit visible dans la vue détaillée de la cellule dans le 1er encadré"). Réutilise `db.add_defaut` (donc le workflow défaut existant : visible dans l'historique des défauts, dans le panneau "en attente", résolvable via `/api/maintenance/intervention`) plutôt que d'inventer un second mécanisme de signalement parallèle. |
| **CHG-V2-066** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : l'Opérateur ne pouvait consulter ni l'historique des défauts ni l'historique de ses propres demandes de maintenance (pages entièrement réservées au rôle MAINTENANCE). `security.require_role` accepte désormais plusieurs rôles (`require_role("MAINTENANCE", "OPERATEUR")`, signature variadique rétrocompatible). `GET /api/faults/history` et `GET /api/maintenance/history` ouverts en lecture à l'Opérateur ; pour ce dernier, un filtre par auteur (`username_auteur`) est **forcé côté serveur** dès que le rôle n'est pas MAINTENANCE (jamais fourni/contrôlé par le client), pour qu'un Opérateur ne voie jamais les demandes d'un autre utilisateur. `fault_history.html` et `maintenance_history.html` ouvertes à l'Opérateur : le bouton "Intervenir" reste conditionné à `currentRole === 'MAINTENANCE'` (déjà le cas), et le panneau "Demandes en attente" (outil de travail Maintenance) est masqué pour l'Opérateur, qui ne voit que l'historique de ses propres demandes. La page Données (export CSV inclus, cf. CHG-V2-068) reste réservée à la Maintenance, conformément à la demande explicite de l'utilisateur. | Réponse directe à l'utilisateur ("il faut que l'opérateur puisse voir l'historique de ses demandes de maintenance et historique des défauts" / "l'opérateur ne peut pas télécharger les données en csv"). Le filtrage par auteur côté serveur (plutôt qu'un simple masquage côté client) est un choix de sécurité délibéré : un Opérateur qui inspecterait les requêtes réseau ne doit pas pouvoir récupérer l'historique d'un collègue en modifiant les paramètres de la requête. |
| **CHG-V2-067** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : le bouton "Demander une intervention Maintenance" (`dashboard.html`) n'était visible pour l'Opérateur que si les seuils automatiques l'exigeaient déjà (temps avant maintenance ≤ 10h ou défaut actif) — un changement introduit par une décision antérieure ("n'est plus obligatoire !"). Le bouton est désormais **toujours visible** pour l'Opérateur, sur chaque cellule, quel que soit son état ; seule une demande déjà active sur cette cellule précise le masque (remplacé par la confirmation "Intervention demandée"), pour éviter les doublons. | Réponse directe à l'utilisateur ("je n'ai pas de bouton pour faire de demande de maintenance en tant qu'opérateur"). L'Opérateur doit pouvoir signaler un problème qu'il constate lui-même sur le terrain, même si aucun seuil automatique ne l'a encore détecté — la précédente restriction empêchait précisément ce cas d'usage. |
| **CHG-V2-068** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un onglet "Télécharger" dédié dans `data_comparison.html` (bascule client, sans rechargement), à côté de l'onglet "Courbes" existant : un CSV par graphique (température/courant/couple/pression/lubrifiant, colonnes Horodatage + une colonne par cellule, mêmes valeurs que celles tracées) et un CSV par cellule (relevés bruts non moyennés des 6 axes). Génération et téléchargement entièrement côté client (`Blob`/`URL.createObjectURL`, BOM UTF-8 pour un affichage correct des accents dans Excel FR) à partir des données déjà chargées par `loadAll()`, sans nouvel appel réseau ni endpoint backend dédié. Page réservée à la Maintenance (inchangé). | Réponse directe à l'utilisateur ("télécharger chaque tableau de courbes, et de données pour chaque partie/cellule, en csv, avec un onglet exprès"). Génération côté client choisie plutôt qu'un export serveur : les données affichées sont déjà en mémoire (mêmes valeurs que les courbes), un aller-retour réseau supplémentaire n'aurait rien apporté. |
| **CHG-V2-069** | 2026-08-20 | 🌐 [IT - Web] | Ajout du bouton d'agrandissement (⤢, réutilisant `openChartZoom` de `common.js`) sur les 5 graphiques de `data_comparison.html`, absent depuis l'introduction de cette page (jamais aligné avec `cell_detail.html`, qui l'a depuis CHG-V2-043/044). `openChartZoom` accepte désormais un paramètre `labels` optionnel : `data_comparison.html` doit le fournir explicitement (labels fusionnés/triés, cf. INC-V2-021) sous peine de réintroduire les sauts en arrière dans la version agrandie ; `cell_detail.html` continue de l'omettre (une seule série par axe, pas besoin de fusion). | Voir INC-V2-023 (bug de parité fonctionnelle signalé par l'utilisateur : "on ne peut pas agrandir les courbes"). |
| **CHG-V2-070** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : un signalement critique de la Maintenance (CHG-V2-065) ne créait qu'une ligne d'historique, sans jamais mettre la cellule à l'arrêt côté supervision — contrairement à un défaut remonté par l'automate. `report_manual_fault()` (`opcua_client.py`, factorisée avec l'action de simulation `force_fault` via `_write_fault_state()`) écrit désormais le même état OPC UA qu'un défaut réel (`EnDefaut=True`, `Etat=DEFAUT`, alarme = description du signalement, cycle remis à 0) dès que `POST /api/maintenance/report-issue` reçoit `severity=critique` ; résilient à une panne OPC UA ponctuelle (le défaut reste de toute façon persisté en base). Symétriquement, `POST /api/maintenance/intervention` lève cet état (`ack_fault`) quand le défaut résolu est ce type de signalement critique **et** qu'aucun autre signalement critique de la Maintenance n'est encore actif sur la même cellule (pour ne pas rouvrir à tort une cellule qui a encore un problème en cours). | Réponse directe à l'utilisateur ("si la maintenance met une alerte critique la machine doit être en arrêt comme si il y avait un défaut"). Réutilise l'état OPC UA "défaut" déjà existant (dashboard rouge, cycle stoppé, alerte bannière) plutôt que d'inventer un état parallèle, pour un comportement strictement identique à un défaut automatique aux yeux de l'utilisateur. |
| **CHG-V2-071** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : le bouton "Faire la maintenance"/"Maintenance effectuée" (`ack_maint`) fonctionnait à tout moment, y compris pour "terminer" une maintenance non due, ce qui pouvait aussi masquer à tort un arrêt en cours. Nouvelle fonction `opcua_client.is_maintenance_due()` (lit le noeud `MaintenanceRequise`, retourne `False` par défaut si l'OPC UA est illisible) appelée par `POST /api/simu/action` avant d'exécuter `ack_maint` : rejeté avec `409` sinon. Côté `dashboard.html`, le bouton contextuel par cellule ("Maintenance effectuée") n'apparaît désormais que si `cell.maint_req` est vrai (nouvelle variable `maintDue`, distincte de `isMaintReq` qui inclut l'alerte précoce à ≤ 10h conservée pour la bannière/le style). Le bouton du panneau de simulation (POC, toujours visible pour tester manuellement) reste affiché mais devient un no-op avec message d'erreur explicite tant que la maintenance n'est pas due. | Réponse directe à l'utilisateur ("même si on clique sur effectuer maintenance ... cela ne doit pas fonctionner" / "ce bouton doit apparaître que pour le moment où la maintenance heures a atteint 0"). Contrôle appliqué côté serveur (pas seulement en masquant le bouton) : un clic direct sur l'API sans passer par l'interface reste bloqué. |
| **CHG-V2-072** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : aucun moyen de résoudre individuellement, avec message optionnel, les problèmes actifs listés dans le 1er encadré de `cell_detail.html` (sous "Signaler un problème", cf. CHG-V2-065) — seule la page globale Historique des défauts le permettait. Ajout d'un bouton "Intervenir" par problème actif dans ce même encadré, ouvrant une fenêtre modale identique à celle de `fault_history.html` (résolu / pris en charge reste actif + notes optionnelles), réutilisant le même endpoint `POST /api/maintenance/intervention` (aucune duplication de logique backend). | Réponse directe à l'utilisateur ("il faut un bouton sur chaque problème signalé pour les régler 1 par 1 et mettre un message si besoin"). |
| **CHG-V2-073** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : suppression de la ligne "Température Axes" du 1er encadré de `cell_detail.html`, et renommage de cet encadré ("Diagnostics avancés" → "Informations générales", FR/EN) puisqu'il ne contient plus de diagnostic technique par axe mais l'état des problèmes actifs et les actions Maintenance (signaler/intervenir). | Demande utilisateur explicite ("je veux que tu supprimes la température axe" / "je veux que tu renommes ce panneau ... en Informations Générales"). |
| **CHG-V2-074** | 2026-08-20 | 🌐 [IT - Web] | Signalé par l'utilisateur : la grille de statut des cartes `dashboard.html` (robot/pneumatique/lubrifiant) utilisait `font-family: monospace` ("style terminal"), dont la largeur de caractère plus importante faisait passer "PNEUMATIQUE (6.2 bar)" sur deux lignes de façon décalée par rapport aux colonnes voisines. Retrait du `monospace` (retour à la police proportionnelle du site) et restructuration de la colonne pneumatique en 2 lignes label/valeur (nouvelle clé i18n `pressureLabel`), identique en forme aux colonnes robot et lubrifiant, quelle que soit la longueur du contenu. | Réponse directe à l'utilisateur ("il faut revoir la police d'écriture de robot kuka, lub, pression. et mettre sur la même ligne pression et lub (pas l'écriture décalée)"). |
| **CHG-V2-075** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un badge d'alerte directement sur chaque carte du dashboard (Maintenance ET Opérateur), signalant qu'une anomalie Maintenance est active sur cette cellule sans avoir à ouvrir sa vue détaillée — y compris une sévérité "avertissement", jusque-là invisible en dehors de `cell_detail.html`. `GET /api/status` regroupe désormais les défauts non résolus par cellule (`active_issues`, nouvelle fonction `_active_issues_by_cell()` dans `web_server.py` : compte + sévérité max), consommé côté `dashboard.html` pour afficher un badge orange ("Anomalie signalée (n)") ou rouge ("Alerte critique (n)"). | Réponse directe à l'utilisateur ("il faudrait aussi avoir un indicateur pour qu'on sache si il y a une alerte de la maintenance sur une cellule sans avoir à aller dans la vue détaillée" / "si je déclenche une alerte sur une cellule, l'opérateur ne peut pas le savoir"). |
| **CHG-V2-076** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'une sous-catégorie "Demande d'intervention opérateur" dans le panneau "Informations générales" de `cell_detail.html`, distincte du bloc "Problèmes actifs" : affiche la demande générique (`active_maintenance_requests`, si présente pour la cellule affichée, via `data.maint_requests` déjà renvoyé par `/api/status`) avec l'auteur et le message éventuel, et un bouton "Prise en charge" (Maintenance uniquement) réutilisant `/api/maintenance/acknowledge-request` — même flux que sur la page Historique Maintenance. | Réponse directe à l'utilisateur ("il faut également que si opé demande intervention maintenance, qu'elle apparaisse dans informations générales, une sous catégorie"). |
| **CHG-V2-077** | 2026-08-20 | 🌐 [IT - Web] | Correction d'un bug signalé par l'utilisateur : l'action `ack_maint` ("Maintenance effectuée", quand le compteur atteint 0) effaçait à tort, via `active_maintenance_requests.pop(cell_id, None)`, une éventuelle demande générique d'intervention lancée séparément par l'Opérateur sur la même cellule — deux concepts indépendants (compteur de maintenance préventive vs. demande ponctuelle de l'opérateur) conflatés à tort. Ce `pop()` a été retiré de la branche `ack_maint` de `POST /api/simu/action` ; la demande générique ne disparaît plus que via une prise en charge explicite (`/api/maintenance/acknowledge-request`). | Réponse directe à l'utilisateur ("si je fais maintenance effectuée et que j'ai une demande d'intervention lancée par un opé, ça dit que j'ai aussi fait la demande de maintenance de l'opérateur ce qui n'est pas forcément le cas"). |
| **CHG-V2-078** | 2026-08-20 | 🌐 [IT - Web] | Refonte des exports CSV de `data_comparison.html` : suppression de la section "Télécharger par graphique (3 cellules)" (comparatif), jugée inutile par l'utilisateur ; le CSV par cellule (section restante, seule) passe d'un format "long" (une ligne par mesure d'axe, 6 lignes par horodatage, sans pression ni lubrifiant) à un format "large" : une ligne par horodatage, une colonne par mesure (Horodatage, puis Température/Courant/Couple × 6 axes, puis Pression pneumatique et Niveau lubrifiant). Corrige le rendu "très mal formaté" signalé (répétitions de lignes, données pression/lubrifiant absentes). | Réponse directe à l'utilisateur ("les téléchargements CSV rendent très mal niveau format. Et je veux pouvoir télécharger cellule par cellule, pas un comparatif des 3"). |
| **CHG-V2-079** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un bouton de téléchargement d'image (PNG, icône ⬇ à côté du bouton d'agrandissement ⤢) sur chaque courbe de `data_comparison.html` et `cell_detail.html`. Nouvelle fonction partagée `downloadChartAsImage()` (`common.js`) : compose le canvas Chart.js sur un fond blanc opaque avant export (le canvas Chart.js est transparent par défaut, illisible une fois imprimé/collé sur fond blanc). Réservé au rôle Maintenance (bouton masqué pour l'Opérateur), au même titre que les autres téléchargements. | Réponse directe à l'utilisateur ("pour les courbes j'aimerais que l'on puisse télécharger une image de la courbe"). |
| **CHG-V2-080** | 2026-08-20 | 🌐 [IT - Web] | Correction d'un bug signalé par l'utilisateur : "Acquitter le défaut" du panneau de simulation POC (`ack_fault`) levait n'importe quel état de défaut OPC UA, y compris une anomalie **critique signalée par la Maintenance** (CHG-V2-070) — ces deux origines partagent le même état OPC UA plat (`EnDefaut`), sans métadonnée d'origine. Nouvelle fonction `_has_active_manual_critical_issue()` (`web_server.py`, réutilisée aussi par `/api/maintenance/intervention`, qui faisait déjà une vérification équivalente en ligne) : si une anomalie critique Maintenance est encore active sur la cellule, `POST /api/simu/action?action=ack_fault` est désormais rejeté avec `409` — seul le bouton "Intervenir" dédié (par anomalie) reste habilité à la lever. Un défaut simulé via "Forcer un défaut" (POC) reste acquittable normalement. | Réponse directe à l'utilisateur ("si je crée une alerte critique sur une cellule et que je clique sur acquitter, ça enlève l'alerte, je ne veux pas — ça doit fonctionner que si c'est un test de défaut via le POC"). |
| **CHG-V2-081** | 2026-08-20 | 🌐 [IT - Web] | Ouverture en lecture de la vue détaillée de cellule (`/cell/{id}`, y compris `GET /api/cell/{id}/axes` et `/api/cell/{id}/measures`, désormais `require_role("MAINTENANCE", "OPERATEUR")`) et de l'onglet "Courbes" de `/donnees` (lien "Données" ajouté au menu Opérateur dans `common.js`) au rôle Opérateur. Conformément à la réponse de l'utilisateur à la question de clarification posée ("Résolution réservée Maintenance"), l'Opérateur voit tout (problèmes actifs, historique, axes, courbes) mais ne peut ni signaler un problème, ni intervenir/prendre en charge, ni télécharger (CSV ou image de courbe) — ces actions restent strictement réservées à la Maintenance, boutons masqués côté client en plus des vérifications serveur déjà en place. **Décision prise sans confirmation explicite de l'utilisateur** : l'ouverture de l'onglet Courbes de `/donnees` (au-delà des seules "vues détaillées des cellules" citées littéralement) s'appuie sur la phrase "la seule chose que l'opérateur ne peut pas faire c'est créer des alertes, ni télécharger les graphs", qui implique une consultation possible des courbes de comparaison — à confirmer/ajuster si ce n'était pas l'intention. | Réponse directe à l'utilisateur ("il faudrait que les opérateurs aient accès aux vues détaillées des cellules. La seule chose que l'opérateur ne peut pas faire c'est créer des alertes, ni télécharger les graphs") et à la réponse "Résolution réservée Maintenance (Recommandé)" à la question de clarification posée sur les actions de résolution. |
| **CHG-V2-082** | 2026-08-20 | 🌐 [IT - Web] | Correction du bug de courbes "illisibles" signalé par l'utilisateur (persistant depuis plusieurs rounds) : nouvelle fonction partagée `decimateSeries()` (`common.js`) qui moyenne une série temporelle triée par paquets de points bruts consécutifs (paquet = `ceil(nb_points / 200)`), utilisée par `cell_detail.html` (courbes par axe et pression/lubrifiant) et `data_comparison.html` (courbes moyennées 3 cellules) avant tout rendu à l'écran ou export d'image — jamais pour l'export CSV, qui continue d'utiliser les données brutes complètes. Root-cause diagnostiquée par requête directe sur la base SQLite de production : les pics correspondent à des redémarrages ponctuels du serveur OPC UA (`opcua_server.py`), qui réinitialisent momentanément les 6 axes à leur valeur "idle" de départ pour une seule mesure ; le moyennage par paquets dilue mécaniquement cette valeur isolée (poids 1/N dans son paquet) tout en résolvant la surcharge de points (échantillonnage 15s sur une fenêtre 24h/7j/30j, plusieurs milliers de points pour quelques centaines de pixels de large). | Réponse directe à l'utilisateur ("toujours des problèmes avec des pics sur les données dans les courbes. des points de courbes pas réguliers. et les traits sont pas lisibles"). |
| **CHG-V2-083** | 2026-08-20 | 🌐 [IT - Web] | Le téléchargement d'image de courbe (`downloadChart()`, `cell_detail.html` et `data_comparison.html`) produit désormais l'image de la version **agrandie** de la courbe (mêmes options que le zoom ⤢, résolution 1600×800), et non plus une capture de la petite vignette en ligne. Nouvelle fonction partagée `downloadZoomedChartImage()` (`common.js`) : construit un graphique Chart.js temporaire sur un canvas hors écran (jamais attaché au DOM) avec les options de la fenêtre agrandie, télécharge l'image via `downloadChartAsImage()` (CHG-V2-079, inchangée), puis le détruit. Nouvelle fonction `buildZoomOptions()` factorisée dans chaque page pour que le rendu à l'écran (`zoomChart()`) et l'image téléchargée ne puissent jamais diverger. | Réponse directe à l'utilisateur ("Quand on télécharge l'image il faut que ce soit l'image de quand on agrandit la courbe"). |
| **CHG-V2-084** | 2026-08-20 | 🌐 [IT - Web] | Correction d'un défaut d'alignement introduit par le badge d'alerte du dashboard (CHG-V2-075) : sur une carte dont l'en-tête passe sur 2 lignes (badge "Anomalie signalée"/"Alerte critique"), le bouton "Demander une intervention Maintenance" se retrouvait plus bas que sur les cartes voisines de la même ligne, sans badge. `.dashboard .card` passe en `display:flex; flex-direction:column` et le bloc d'actions de bas de carte (lien détail, bouton opérateur, actions Maintenance) est désormais enveloppé dans `<div class="card-actions">` (`margin-top:auto`), ce qui l'ancre en bas de carte quelle que soit la hauteur du contenu au-dessus — la grille CSS étirant déjà chaque carte à la hauteur de la plus grande de sa ligne. Changement scopé à `.dashboard .card` uniquement (pas la classe `.card` globale), pour ne pas risquer de perturber le redimensionnement des cartes à graphique (`<canvas>`/Chart.js) de `cell_detail.html` et `data_comparison.html`. | Réponse directe à l'utilisateur ("à cause du signal anomalie, le demander intervention est plus bas que les autres"). |
| **CHG-V2-085** | 2026-08-20 | 🌐 [IT - Web] | Correction d'une régression apparue juste après CHG-V2-082/083 : "il n'y a plus aucune courbe et on ne peut plus télécharger les courbes". Root-cause : `common.js` (comme `theme.css`) est servi via `/static` (`StaticFiles`), qui ne porte pas les en-têtes anti-cache appliqués aux pages HTML (`_html_page()`) — le navigateur continuait donc de servir une copie de `common.js` mise en cache avant l'ajout de `decimateSeries()`/`downloadZoomedChartImage()`, provoquant une `ReferenceError` silencieuse (absorbée par les `try/catch` existants) qui interrompait tout le rendu des courbes ET les téléchargements sur la page. Nouvelle fonction `web_server.py::_static_url()`, exposée comme global Jinja2 `static_v(filename)`, qui suffixe chaque URL `/static/...` d'un paramètre de version basé sur la date de modification du fichier sur disque (`?v=<mtime>`) : le navigateur recharge alors systématiquement la dernière version dès qu'un asset partagé change, sans intervention manuelle ni cache-bust à retenir à chaque round. Appliqué aux 5 templates (`theme.css`/`common.js`, plus `chart.umd.js` sur les 2 pages à courbes). Nécessite un redémarrage du serveur `uvicorn` (lancé sans `--reload`) pour prendre effet, le nouveau global Jinja n'existant pas en mémoire tant que `web_server.py` n'est pas rechargé. | Réponse directe à l'utilisateur ("il n'y a plus aucune courbe et on ne peut plus télécharger les courbes") — diagnostiqué en simulant `decimateSeries`/`buildAxisDatasets` avec des données réelles issues de la base SQLite de production (aucune erreur), ce qui a écarté un bug de logique et orienté vers un problème d'assets non rafraîchis. |
| **CHG-V2-086** | 2026-08-20 | 🌐 [IT - Web] | Correction du "retour en arrière / double épaisseur" par endroits signalé sur les courbes par axe de `cell_detail.html`. Root-cause confirmée par requête SQL directe sur la base de production : les 6 axes sont mesurés au même tick de simulation mais écrits en base via des `INSERT` distincts, dont l'horodatage (précision à la seconde) peut différer d'1s d'un axe à l'autre (~50 décalages constatés sur ~1880 mesures/axe sur 24h) ; une fois chaque axe décimé indépendamment (CHG-V2-082), ce léger décalage suffisait à faire ajouter par Chart.js (échelle `category` sans "labels" explicites) l'horodatage propre à un axe en fin de liste de catégories plutôt qu'à sa place chronologique, provoquant un bond en arrière du tracé de cet axe. `mergedSortedTimes()` (déjà utilisée par `data_comparison.html` pour INC-V2-021) est promue dans `common.js` et `cell_detail.html` fournit désormais une liste de catégories fusionnée et triée explicite à ses 3 courbes par axe (température/courant/couple), à l'écran comme à l'agrandissement et au téléchargement. Corrigé sans toucher au format d'écriture en base (hors périmètre de cette correction). | Réponse directe à l'utilisateur ("courbe revenue mais par endroit elles sont bizarres (retour en arrière ou double épaisseur)") — diagnostiqué par comparaison directe des horodatages bruts entre deux axes d'une même cellule (aucune inversion par `id`, mais des horodatages non alignés entre axes), puis vérifié après correctif par simulation Node sur les données réelles (0 inversion d'indice de catégorie sur les 6 axes). |
| **CHG-V2-087** | 2026-08-20 | 🌐 [IT - Web] | Refonte de `data_comparison.html` (renommée "Données par cellule") : suppression du graphique comparatif fusionnant les 3 cellules sur une seule courbe (température/courant/couple/pression/lubrifiant), remplacé par une section indépendante par cellule reconstruite dynamiquement (même principe que les cartes du dashboard principal). La température n'est plus moyennée entre les 6 axes d'une cellule : chaque axe a désormais sa propre courbe (nouvelle fonction `buildTempDatasetsForCell()`, même logique de décimation + fusion de catégories que `cell_detail.html`, cf. CHG-V2-086). Courant et couple restent des moyennes par cellule (aucune précision demandée sur ces deux mesures dans la demande utilisateur) ; à ajuster si ce n'était pas l'intention. Les boutons de téléchargement d'image, désormais générés dynamiquement, ne sont inclus dans le HTML que pour le rôle Maintenance (plutôt que masqués après coup), pour éviter un masquage devenu sans effet une fois les cartes reconstruites à chaque chargement. | Réponse directe à l'utilisateur ("je ne veux pas d'un graphique avec les 3 cellules. Je veux les cellules indépendantes et pour la température des axes, il faut que ce soit chaque axe séparément"). **Décision prise sans confirmation explicite** : courant/couple restent moyennés par cellule (non précisé par l'utilisateur) — à confirmer si un éclatement par axe est aussi souhaité pour ces deux mesures. |
| **CHG-V2-088** | 2026-08-20 | 🛡️ [Global] | Ajout du rôle **ADMIN** : `security.py::require_role()` laisse désormais passer systématiquement un utilisateur ADMIN, quels que soient les rôles demandés par l'endpoint (un seul point de bypass, sans toucher aux 9 appels `require_role(...)` existants). Nouvelle table `Cellule` (`db.py`) — id/nom/type_robot/adresse_ip — amorcée avec les 3 cellules jusque-là codées en dur (mêmes id/noms/IP, pour la continuité avec l'historique déjà en base), source de vérité désormais partagée entre `web_server.py` et `opcua_server.py` (processus séparés). Nouveaux points d'API réservés à ADMIN : `POST /api/admin/users` (crée un compte OPERATEUR ou MAINTENANCE — volontairement pas ADMIN, cf. CHG-V2-091 ci-dessous), `GET /api/admin/users`, `POST /api/admin/cells`. Nouveau `GET /api/cells` (ouvert à tout rôle authentifié) consommé par `common.js::loadCellNames()` pour peupler dynamiquement `CELL_NAMES`/`CELL_LIST`, qui remplacent l'ancien objet `CELL_NAMES` figé à 3 entrées. | Réponse directe à l'utilisateur ("j'aimerai qu'il y ait un compte administrateur, où on peut créer des comptes (opérateur ou maintenance) et que l'on puisse créer des nouvelles cellules de robots"), avec deux clarifications obtenues via question à choix multiples avant implémentation : "Admin = tous les droits" (d'où le bypass générique dans `require_role`) et "Ajout en base + redémarrage simulateur" pour la création de cellules (cf. CHG-V2-090). |
| **CHG-V2-089** | 2026-08-20 | 🌐 [IT - Web] | Nouvelle page `/administration` (`admin.html`, réservée ADMIN côté serveur ET côté client) : formulaire de création de compte (identifiant, mot de passe, rôle Opérateur/Maintenance) avec liste des comptes existants, et formulaire de création de cellule (nom, type de robot, adresse IP) avec liste des cellules existantes et bandeau d'avertissement "redémarrage du simulateur requis". Lien "Administration" ajouté au menu de navigation (`renderTopNav()`), visible uniquement pour ADMIN. Mots de passe saisis côté admin hashés via bcrypt (nouvelle fonction `security.py::hash_password()`) avant stockage — jamais transmis ni conservés en clair, au même titre que les comptes de démo existants. | Volet interface de CHG-V2-088, suivant les conventions déjà établies par les autres pages du site (popups `showModal`/`openTextPromptModal` au lieu d'`alert()`, i18n FR/EN, `{{ static_v(...) }}` pour les assets, en-têtes anti-cache `_html_page()`). |
| **CHG-V2-090** | 2026-08-20 | ⚙️ [OT - OPC UA] 🌐 [IT - Web] | Les 3 cellules codées en dur (`range(1, 4)`, listes `robot_types`/`cell_names`/`cell_ips`) sont remplacées par une lecture de la table `Cellule` au démarrage de `opcua_server.py` (`db.init_db()` + `db.list_cellules()`) et à chaque appel de `fetch_opcua_data()`/`/api/status` côté `web_server.py`. `opcua_client.py::fetch_opcua_data()` prend désormais `cell_ids: list[int]` en paramètre et isole chaque cellule dans son propre `try/except` : une cellule ajoutée en base mais absente du serveur OPC UA en cours d'exécution (simulateur pas encore redémarré) est simplement ignorée pour ce cycle plutôt que de faire échouer la lecture de TOUTES les cellules. Le sélecteur de cellule figé à 3 options (`data_comparison.html`, `fault_history.html`, `maintenance_history.html`) est remplacé par une liste construite dynamiquement depuis `CELL_LIST` (`common.js::loadCellNames()`, threadé dans l'initialisation de chacune de ces 3 pages). | Rend effective la décision utilisateur "Ajout en base + redémarrage simulateur" (CHG-V2-088) : une nouvelle cellule créée depuis `/administration` apparaît immédiatement dans les listes/filtres du dashboard (id + nom en base), mais ses données OPC UA (température, état, défauts...) ne sont disponibles qu'après redémarrage de `python -m src_v2.opcua_server` — comportement vérifié par simulation (démarrage réel du simulateur avec une base de test, puis appel de `fetch_opcua_data()` avec un id de cellule absent du serveur : les autres cellules restent disponibles, la cellule manquante est journalisée en warning et ignorée). |
| **CHG-V2-091** | 2026-08-20 | 🛡️ [Global] | Ajout d'un compte ADMIN d'amorçage (`db.py::_SEED_USERS`, identifiant `admin`, mot de passe `admin123`) : sans lui, personne ne pourrait jamais se connecter en ADMIN pour créer les premiers comptes/cellules, la page `/administration` exigeant elle-même déjà un compte ADMIN. `init_db()` détecte spécifiquement l'absence d'un rôle ADMIN (et pas seulement une table vide) pour amorcer ce compte même sur une base déjà existante (comptes Opérateur/Maintenance créés avant l'ajout de ce rôle), sans toucher aux comptes déjà en place — vérifié par simulation d'une base pré-existante sans ADMIN. **Décision prise sans confirmation explicite** : mot de passe volontairement simple (même niveau que les comptes de démo `ope123`/`maint123`), à changer en priorité vu que ce compte a tous les droits — aucun mécanisme de changement de mot de passe n'existe encore dans l'application (hors accès direct à la base). |
| **CHG-V2-092** | 2026-08-20 | 🌐 [IT - Web] | Correction de l'échelle absurde (`"6,20000000000000010"`) affichée sur les courbes de pression pneumatique/lubrifiant, y compris sur le dashboard après ajout d'une 4e cellule (retour utilisateur, capture d'écran à l'appui). Root cause identifiée par requête SQL directe (valeur réellement constante en base, ex. `6.2`) et relecture de `opcua_server.py` : `pneumatic_pressure_var`/`lubrix_level_var` sont écrites une seule fois à la création de la cellule et jamais réécrites par la boucle de simulation (contrairement à température/courant/couple/progress/vibration), donc la valeur ne varie jamais réellement — c'est la division JS de la moyenne (`decimateSeries()`, `common.js`) qui introduit un bruit de dernière décimale binaire (`6.2` devient `6.200000000000001` sur certains paquets de points et pas d'autres), que Chart.js zoome ensuite en absurde. Fix : arrondi à 2 décimales du résultat de la moyenne dans `decimateSeries()` — un seul correctif partagé par toutes les courbes qui l'utilisent (pression, lubrifiant, température, courant, couple, sur `cell_detail.html` ET `data_comparison.html`). Vérifié par simulation Node.js (3000 valeurs identiques en entrée → une seule valeur distincte en sortie après correctif). La variation observée par l'utilisateur entre cellules (certaines touchées, d'autres non) s'explique par le seuil de décimation (`maxPoints = 200`) : en dessous, les valeurs brutes passent sans moyenne et le bug n'apparaît pas — la cellule ajoutée avait moins d'historique, changeant les frontières de paquets. | Réponse directe à l'utilisateur ("l'échelle de la pression est pas bonne dans les différentes cellules" + "vu que j'ai rajouté une cellule l'échelle n'est plus bonne avec pression et lub sur le dashboard") : les deux symptômes partagent la même cause racine, corrigée par un seul changement. |
| **CHG-V2-093** | 2026-08-20 | 🌐 [IT - Web] | Courant et couple sont désormais éclatés par axe (6 courbes chacun) sur `data_comparison.html`, au même titre que la température (cf. CHG-V2-087) : la fonction `buildTempDatasetsForCell()` est généralisée en `buildAxisDatasetsForCell(axisMeasures, field)`, appelée 3 fois (`temperature_c`, `courant_a`, `couple_nm`) ; `averageAxesByTime()` (moyenne par cellule) n'est plus utilisée et a été supprimée. | Réponse directe à l'utilisateur ("je veux que la consommation et le couple soient également par axe"), qui lève l'incertitude explicitement notée dans CHG-V2-087 ("à confirmer si un éclatement par axe est aussi souhaité pour ces deux mesures") — la réponse est oui, les deux mesures suivent désormais le même traitement que la température. |
| **CHG-V2-094** | 2026-08-20 | 🌐 [IT - Web] | Ajout d'un téléchargement CSV sur `fault_history.html` (historique des défauts) et `maintenance_history.html` (historique des interventions), réservé à la Maintenance/Admin (`hasMaintenanceRights()`), sur le même modèle que les téléchargements d'images de courbes déjà existants. Les fonctions `toCsvValue()`/`downloadCsv()` (échappement CSV + génération/déclenchement du fichier via Blob, BOM UTF-8 pour un import correct dans Excel) existaient déjà en local dans `data_comparison.html`, où elles servent au téléchargement CSV des mesures ; elles sont promues dans `common.js` (DRY, même principe déjà appliqué à `mergedSortedTimes()`) pour être réutilisées par les 3 pages. | Réponse directe à l'utilisateur ("Je souhaite que l'on puisse télécharger les historiques de pannes et de maintenance"). |
| **CHG-V2-095** | 2026-08-20 | 🌐 [IT - Web] | Correction du lien "Voir le détail" (vers `/cell/{id}`) sur les cartes du dashboard, qui n'était généré que pour la Maintenance : il était imbriqué par erreur dans le même bloc conditionnel `if (hasMaintenanceRights(currentRole))` que les boutons d'action Maintenance, alors que la page `/cell/{id}` et ses endpoints (`/api/cell/{id}/axes`, `/measures`) acceptaient déjà le rôle OPERATEUR côté serveur. Investigation ayant confirmé que tout le reste (gating serveur, page elle-même) était déjà correct : seul ce lien était concerné, corrigé en le sortant de la condition. | Réponse directe à l'utilisateur ("Je souhaite que l'opérateur puisse avoir accès à la vue détaillé de chaque cellule"). |
| **CHG-V2-096** | 2026-08-20 | 🌐 [IT - Web] | Trois nouvelles capacités sur `/administration` (ADMIN uniquement, côté serveur ET client) : (1) changement du rôle d'un compte existant (Opérateur ⇄ Maintenance) via un menu déroulant sur chaque ligne du tableau des comptes (`PATCH /api/admin/users/{username}`) ; (2) modification d'une cellule (nom/type de robot/adresse IP) via un formulaire d'édition inline (`PATCH /api/admin/cells/{id}`) ; (3) suppression d'une cellule (`DELETE /api/admin/cells/{id}`), confirmée via une nouvelle popup de confirmation générique (`openConfirmModal()`/`closeConfirmModal()`, ajoutée à `common.js` sur le modèle de `openTextPromptModal()` déjà existant, pour remplacer un `confirm()` natif). Deux garde-fous appliqués au changement de rôle (`db.py::update_user_role()`) : impossible de cibler le rôle ADMIN (promotion bloquée, seuls `CREATABLE_ROLES` acceptés) et impossible de modifier un compte dont le rôle ACTUEL est ADMIN (anti-auto-verrouillage / anti-rétrogradation accidentelle). La suppression d'une cellule (`db.py::delete_cellule()`) ne purge PAS son historique associé (mesures/défauts/interventions) — choix délibéré, documenté dans le code, pour préserver la traçabilité même après suppression d'une cellule (aucune contrainte de clé étrangère ne le liait de toute façon, par conception). `GET /api/cells` retourne désormais aussi `type_robot`/`adresse_ip` (nécessaire pour pré-remplir le formulaire d'édition). Vérifié par une suite de tests `TestClient` couvrant chaque cas (succès, rôle/cellule introuvable, promotion ADMIN refusée, changement sur un compte ADMIN refusé, rôle non-admin refusé). | Réponse directe à l'utilisateur ("En tant qu'admin dans la page d'administration je dois aussi pouvoir changer les roles des comptes déjà existants. Il faut également que je puisse supprimer une cellule si besoin ou la modifier."). |
| **CHG-V2-097** | 2026-08-20 | 🌐 [IT - Web] | Deux compléments sur les demandes d'intervention génériques (bouton "Demander une intervention" côté Opérateur, sans défaut associé) : (1) un badge d'alerte (`&#9888; Intervention demandée`) apparaît désormais directement sur la carte du dashboard, à côté de l'IP de la cellule, visible pour l'Opérateur ET la Maintenance dès qu'une demande générique est active — avant ce correctif, seule une bannière réservée à la Maintenance et un texte de confirmation pour l'Opérateur signalaient la demande, sans aucune alerte visible sur la carte elle-même pour l'un ou l'autre rôle. (2) `POST /api/maintenance/acknowledge-request` exige désormais un paramètre `probleme_resolu` (booléen, obligatoire), au même titre que `/api/maintenance/intervention` pour un défaut précis : la Maintenance déclare, via une nouvelle popup (`maintenance_history.html`, mêmes choix radio "Problème résolu"/"Pris en charge, reste actif" que la popup d'intervention sur défaut de `fault_history.html`), si la demande est résolue ou seulement prise en charge. Le résultat est tracé sur la ligne d'historique créée (`HistoriqueMaintenance.probleme_resolu`, déjà existant pour les défauts) et affiché avec le même tag `(résolu)`/`(reste actif)` que pour un défaut — `outcomeTag()` a été corrigé pour omettre la mention "sur le défaut #..." quand `defaut_id` est nul (cas d'une demande générique). **Décision documentée** : la demande est retirée d'`active_maintenance_requests` (donc le badge dashboard disparaît) dans les deux cas, résolu ou non — ce dict représente une demande *pas encore prise en charge*, pas le suivi de sa résolution ; une demande "reste active" continue d'être tracée dans l'historique mais ne réapparaît pas comme "en attente". | Réponse directe à l'utilisateur ("Si opérateur fait une demande d'inter : il n'y a pas d'alerte qui apparait sur le dashboard à côté de l'ip pour l'opérateur ni la maintenance. Et quand la maintenance clique sur prise en charge, il faudrait que ce soit comme pour les défauts avec (en cours, ou résolu)."). |
| **CHG-V2-098** | 2026-08-21 | 🛡️ [Global] | Correction de 7 tests en échec dans `tests/test_web_server.py` (76/83 lors de la campagne qualité du matin), causés non par un bug applicatif mais par des doublures de test (mocks/fakes `fetch_opcua_data`) restées sur l'ancienne signature sans paramètre alors que `fetch_opcua_data(cell_ids)` exige désormais la liste des cellules à interroger depuis CHG-V2-088 (création dynamique de cellules par l'Admin), et par deux appels de test à `POST /api/maintenance/acknowledge-request` ne fournissant pas le paramètre `probleme_resolu`, devenu obligatoire depuis CHG-V2-097. Correctifs : ajout du paramètre `cell_ids` (ignoré) aux 5 fonctions de simulation concernées (fixture `client`, `test_status_ok_when_opcua_fails`, `test_sampling_loop_persists_one_iteration_then_stops`) ; passage explicite de `[1, 2, 3]` à l'appel direct de `fetch_opcua_data()` dans `test_fetch_opcua_data_success` ; ajout de `probleme_resolu=true` aux requêtes de `test_generic_request_appears_in_status_until_acknowledged` et `test_acknowledge_unknown_request_returns_404`. Campagne suivante : 83/83 tests passés, couverture inchangée à 77 %. | Régression de maintenance des tests (pas fonctionnelle) détectée lors de la campagne qualité de routine du 21 août 2026 ; corrigée le jour même pour ne pas la laisser masquer une éventuelle vraie régression lors d'une prochaine évolution. |
